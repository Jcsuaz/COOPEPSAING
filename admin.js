// Funciones para la administración
import { auth, db } from './firebase-config.js';
function manageUsers() {
  // Obtener y gestionar usuarios desde Firestore
}

function manageLoans() {
  // Obtener y gestionar préstamos desde Firestore
}
